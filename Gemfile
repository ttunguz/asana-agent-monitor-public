source 'https://rubygems.org'

ruby '3.4.3'

# Standard library gems (explicitly required for modern Ruby environments)
gem 'net-http'
gem 'json'
gem 'logger'
gem 'uri'

group :test do
  gem 'minitest'
  gem 'benchmark'
end
